# ============================================================
# PromoGuard — Environment Variables
# Copy file này thành .env và điền giá trị thực
#   cp .env.example .env
# ============================================================

# ------------------------------------------------------------
# PostgreSQL
# ------------------------------------------------------------
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=

# ------------------------------------------------------------
# Redis
# ------------------------------------------------------------
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=

# ------------------------------------------------------------
# Spring Boot — datasource
# ------------------------------------------------------------
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=

# ------------------------------------------------------------
# Spring Boot — server
# ------------------------------------------------------------
BACKEND_PORT=

# ------------------------------------------------------------
# JWT
# Dùng lệnh sau để sinh secret ngẫu nhiên:
#   openssl rand -base64 64
# ------------------------------------------------------------
JWT_SECRET=replace-with-a-long-random-base64-secret
JWT_EXPIRATION_MS=86400000

# ------------------------------------------------------------
# Email — Resend
# Lấy API key tại: https://resend.com/api-keys
# ------------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
RESEND_FROM_ADDRESS=no-reply@yourdomain.com

# ------------------------------------------------------------
# Email — Gmail SMTP (dùng thay thế Resend nếu cần)
# ------------------------------------------------------------
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# ------------------------------------------------------------
# Railway (CI/CD — chỉ dùng trong GitHub Actions secrets)
# Không điền vào .env local
# ------------------------------------------------------------
# RAILWAY_TOKEN=
